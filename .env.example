# === Database ===
DB_PASSWORD=
DB_PORT=5432

# === Redis ===
REDIS_PORT=6379

# === Application ===
APP_PORT=8080
JWT_SECRET=
KAKAO_CLIENT_SECRET=
NEXON_CLIENT_SECRET=
NEXON_API_KEY_SECRET=

# === Nginx ===
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# === Production ===
APP_IMAGE=ghcr.io/<owner>/<image>:<tag>
BACKUP_DIR=/backups

# === MCP (Claude Code PostgreSQL MCP 연결용) ===
DATABASE_URL=postgresql://maple_user:<DB_PASSWORD값>@localhost:<DB_PORT값>/maple
